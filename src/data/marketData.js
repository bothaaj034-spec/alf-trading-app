/**\n * Generate realistic XAUUSD candlestick data\n */\n\nconst generateCandles = (count = 100, startPrice = 2050) => {\n  const candles = [];\n  let price = startPrice;\n\n  for (let i = 0; i < count; i++) {\n    const change = (Math.random() - 0.5) * 2; // Random change between -1 and +1\n    const open = price;\n    const close = open + change;\n    const high = Math.max(open, close) + Math.random() * 0.5;\n    const low = Math.min(open, close) - Math.random() * 0.5;\n    const volume = Math.floor(Math.random() * 10000) + 5000;\n\n    candles.push({\n      time: new Date(Date.now() - (count - i) * 60000).toISOString(),\n      open: parseFloat(open.toFixed(2)),\n      high: parseFloat(high.toFixed(2)),\n      low: parseFloat(low.toFixed(2)),\n      close: parseFloat(close.toFixed(2)),\n      volume,\n    });\n\n    price = close;\n  }\n\n  return candles;\n};\n\nconst sampleCandles = generateCandles(100, 2050.35);\n\nexport { generateCandles, sampleCandles };\n