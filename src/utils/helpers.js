/**\n * Trading utility functions\n */\n\nexport const formatPrice = (price) => {\n  return parseFloat(price).toFixed(2);\n};\n\nexport const formatPips = (pips) => {\n  return Math.abs(pips).toFixed(1);\n};\n\nexport const calculateRiskReward = (entry, stopLoss, takeProfit) => {\n  if (!entry || !stopLoss || !takeProfit) return 0;\n  const risk = Math.abs(entry - stopLoss);\n  const reward = Math.abs(takeProfit - entry);\n  return risk > 0 ? (reward / risk).toFixed(2) : 0;\n};\n\nexport const calculatePips = (entry, target) => {\n  return (Math.abs(target - entry) * 100).toFixed(1);\n};\n\nexport const getSignalColor = (signal) => {\n  switch (signal) {\n    case 'BUY':\n      return 'text-trading-bullish';\n    case 'SELL':\n      return 'text-trading-bearish';\n    default:\n      return 'text-trading-neutral';\n  }\n};\n\nexport const getSignalBgColor = (signal) => {\n  switch (signal) {\n    case 'BUY':\n      return 'bg-trading-bullish/10';\n    case 'SELL':\n      return 'bg-trading-bearish/10';\n    default:\n      return 'bg-trading-neutral/10';\n  }\n};\n\nexport const calculateWinRate = (winningTrades, totalTrades) => {\n  return totalTrades > 0 ? ((winningTrades / totalTrades) * 100).toFixed(1) : 0;\n};\n\nexport const formatTime = (isoString) => {\n  const date = new Date(isoString);\n  return date.toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit', second: '2-digit' });\n};\n\nexport const formatDate = (isoString) => {\n  const date = new Date(isoString);\n  return date.toLocaleDateString('en-US');\n};\n"